import styles from "../styles/navbar.module.scss"
const NavBar = () => {
  return (
    <nav className={styles.nav}>
            <h1 className={styles.logo}>Stocks</h1>
    </nav>
  )
}

export default NavBar